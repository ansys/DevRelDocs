# Method AddPage
<a id="VM_API_Post_Operations_ApplicationHandler_AddPage_System_String_"></a>

Namespace: [VM.API.Post.Operations](VM.API.Post.Operations.md)  
Assembly: VM.API.Post.Operations.dll  

## <a id="VM_API_Post_Operations_ApplicationHandler_AddPage_System_String_"></a> AddPage\(string\)

Adds and activates a page with the specified name.

```csharp
public IPage AddPage(string name = null)
```

### Parameters

`name` string

The page name. When omitted, the page receives the default name generated by the Postprocessor.

### Returns

 [IPage](VM.Operations.Post.Interfaces.IPage.md)

The newly added [`IPage`](VM.Operations.Post.Interfaces.IPage.md).

### Examples

```python
# Add a page to the document.
page_name = "Page"
page = application_handler.AddPage(page_name)
```

### Remarks

Returned `IPage` is active when the method returns. Previously active `IPage` is deactivated.
