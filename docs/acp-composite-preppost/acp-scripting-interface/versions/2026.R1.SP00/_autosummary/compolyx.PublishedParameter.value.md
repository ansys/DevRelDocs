# value

<a id="compolyx.PublishedParameter.value"></a>

## *property* PublishedParameter.value

The current parameter value. Under WB, setting the value should only be used             for output parameters or to create (initialize) an input parameter.             Otherwise the parameter values in ACP and WB are out of sync.
