# obj_exists

<a id="compolyx.plot_data.ScalarFieldData.obj_exists"></a>

#### ScalarFieldData.obj_exists()

Checks if \_\_obj still references the C++ object (i.e. has not been reset to None by self._delete) and
exists in the graph.
