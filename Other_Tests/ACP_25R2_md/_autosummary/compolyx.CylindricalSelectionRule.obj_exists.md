# obj_exists

<a id="compolyx.CylindricalSelectionRule.obj_exists"></a>

#### CylindricalSelectionRule.obj_exists()

Checks if \_\_obj still references the C++ object (i.e. has not been reset to None by self._delete) and
exists in the graph.
