# getRunInParallel


abstract fun [getRunInParallel](get-run-in-parallel.md)(): [Boolean](https://kotlinlang.org/api/latest/jvm/stdlib/kotlin/-boolean/index.html)

Get whether runs should be in parallel; this is only a hint.

#### Return

whether runs should be run in parallel
