# <a id="VM_Managed_DAFUL_Flexible_Force_PropertyConcentratedLoadBase_DirectionType"></a> Enum PropertyConcentratedLoadBase.DirectionType

Namespace: [VM.Managed.DAFUL.Flexible.Force](VM.Managed.DAFUL.Flexible.Force.md)  
Assembly: VMFlexibleBase.dll  

Specifies type for the direction.

```csharp
public enum PropertyConcentratedLoadBase.DirectionType
```

## Fields

`Radial = 2` 

The radial.

3 function. Fr, Ft(Pri), Ft(Sec)

`Rectangle = 1` 

The rectangle.

3 function. x, y, z.

`Single = 0` 

The single.

1 function. normal direction.

