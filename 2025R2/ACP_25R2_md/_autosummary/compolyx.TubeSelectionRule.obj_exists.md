# obj_exists

<a id="compolyx.TubeSelectionRule.obj_exists"></a>

#### TubeSelectionRule.obj_exists()

Checks if \_\_obj still references the c++ object (i.e. has not been reset to None by self._delete) AND
exists in the graph.
