# Introduction

System Coupling provides a versatile set of commands that enable users to interact with it either through the command-line interface (CLI) or the **Command Console** tab in its graphical user interface (GUI). 

