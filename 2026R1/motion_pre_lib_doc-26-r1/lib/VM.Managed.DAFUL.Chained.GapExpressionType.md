# Enum GapExpressionType
<a id="VM_Managed_DAFUL_Chained_GapExpressionType"></a>

Namespace: [VM.Managed.DAFUL.Chained](VM.Managed.DAFUL.Chained.md)  
Assembly: VMDChainedBase.dll  

Specifies type for assemble method on sprocket.

```csharp
public enum GapExpressionType
```

## Fields

`Off = 0` 

When the segment is located on path, segment length include the gap.



`On = 1` 

When the segment is located, segment length does not include the gap.



