StylesPath = styles
MinAlertLevel = error


# MinAlertLevel = suggestion

Vocab = ansys

Packages = Google

# --- Rule overrides (must be top level, not inside [*.{md}]) ---

#[rule.Vale.Terms]
#level = off

Skipped = _autosummary/**


[rule.Vale.Spelling]
#ignore = [_]

# Ignore all auto-generated API documentation
tokenIgnores = ([A-Za-z]+[A-Z][a-z0-9]+)
ignoreRegExp = ([a-z]+_[a-z0-9_]+)

# Disable this Google rule globally
[rule.Google.Spacing]
level = off


[*.{md}]
# ^ This section applies to only Markdown files.
#
# You can change (or add) file extensions here
# to apply these settings to other file types.
#
# For example, to apply these settings to both
# Markdown and reStructuredText:
#
# [*.{md,rst}]


BasedOnStyles = Vale, Google


# Ignore code spans and fenced code blocks
# TokenIgnores = (\W`[^`]*`), (\W``[^`]*``), (\W```[\s\S]*?```)